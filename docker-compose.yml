services:
  app:
    build: .
    volumes:
      - ./:/app:cached
      - /app/node_modules
    ports:
      - '8080:8080'
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: npm run dev
    stdin_open: true
    tty: true
